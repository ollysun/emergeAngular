/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TariffVersionsComponent } from './tariff-versions.component';

describe('Component: TariffVersions', () => {
  it('should create an instance', () => {
    let component = new TariffVersionsComponent();
    expect(component).toBeTruthy();
  });
});
